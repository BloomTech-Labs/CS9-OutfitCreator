import './billing.css';
import React from 'react';
import Checkout from './Checkout';

const Billing = (props) => {
    return (
        <div>
            <Checkout/>
        </div>
    );
};

export default Billing;